namespace RCDragManagerProd.RoundRobinMode
{
    public static partial class RoundRobinScorecardLogger
    {
        // Writer-related methods (file/IO) can be added here in future.
        // Existing logic does not perform direct file writes, so this
        // partial class currently has no members.
    }
}
