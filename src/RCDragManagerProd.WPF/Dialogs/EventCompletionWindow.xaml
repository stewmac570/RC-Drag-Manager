<Window x:Class="RCDragManagerProd.WPF.Dialogs.EventCompletionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Event complete"
        Width="820" Height="620" MinWidth="620" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="CanResizeWithGrip"
        KeyDown="Window_KeyDown"
        Background="{StaticResource Brush.Background}"
        FontFamily="Segoe UI" ShowInTaskbar="False">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="38" GlassFrameThickness="0"
                      ResizeBorderThickness="5" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- One class: champion in the accent panel, runner-up beside it. -->
        <DataTemplate x:Key="ClassResultTemplate">
            <Border Margin="0,0,0,12" Padding="0"
                    Background="{StaticResource Brush.Surface}"
                    BorderBrush="{StaticResource Brush.Border}" BorderThickness="1"
                    CornerRadius="{StaticResource Radius.Panel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="18,10"
                            BorderBrush="{StaticResource Brush.BorderSubtle}"
                            BorderThickness="0,0,0,1">
                        <TextBlock Text="{Binding ClassName}"
                                   FontSize="{StaticResource FontSize.Lg}" FontWeight="SemiBold"
                                   Foreground="{StaticResource Brush.TextPrimary}"/>
                    </Border>

                    <Grid Grid.Row="1" Margin="18,14,18,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.3*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Padding="16,14"
                                Background="{StaticResource Brush.Primary}"
                                CornerRadius="{StaticResource Radius.Panel}">
                            <StackPanel>
                                <TextBlock Text="★ CHAMPION"
                                           FontSize="{StaticResource FontSize.Xs}" FontWeight="SemiBold"
                                           Foreground="#CCFFFFFF"/>
                                <TextBlock Text="{Binding ChampionName}" Margin="0,6,0,0"
                                           FontSize="{StaticResource FontSize.Xl}" FontWeight="Bold"
                                           Foreground="White" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Padding="16,14"
                                Background="{StaticResource Brush.Background}"
                                BorderBrush="{StaticResource Brush.Border}" BorderThickness="1"
                                CornerRadius="{StaticResource Radius.Panel}">
                            <StackPanel>
                                <TextBlock Text="RUNNER-UP"
                                           FontSize="{StaticResource FontSize.Xs}" FontWeight="SemiBold"
                                           Foreground="{StaticResource Brush.TextHint}"/>
                                <TextBlock Text="{Binding RunnerUpName}" Margin="0,6,0,0"
                                           FontSize="{StaticResource FontSize.Lg}" FontWeight="SemiBold"
                                           Foreground="{StaticResource Brush.TextPrimary}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Border Background="{StaticResource Brush.Background}"
            BorderBrush="{StaticResource Brush.WindowEdge}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="38"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{StaticResource Brush.Chrome}">
                <TextBlock Text="Event complete"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="12" Foreground="{StaticResource Brush.TextHint}" FontWeight="Medium"/>
                <Button Style="{StaticResource Style.WinCtrl.Close}"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Margin="0,0,14,0" Click="BtnClose_Click"/>
            </Grid>

            <Border Grid.Row="1" Padding="24,18"
                    BorderBrush="{StaticResource Brush.BorderSubtle}" BorderThickness="0,0,0,1">
                <StackPanel>
                    <TextBlock Text="{Binding EventName}"
                               FontSize="{StaticResource FontSize.Xxl}" FontWeight="SemiBold"
                               Foreground="{StaticResource Brush.TextPrimary}"/>
                    <TextBlock Text="{Binding SubHeading}" Margin="0,5,0,0"
                               FontSize="{StaticResource FontSize.Md}"
                               Foreground="{StaticResource Brush.TextSecondary}"/>
                </StackPanel>
            </Border>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="24,18">
                <ItemsControl ItemsSource="{Binding Classes}"
                              ItemTemplate="{StaticResource ClassResultTemplate}"/>
            </ScrollViewer>

            <Border Grid.Row="3" Padding="24,14"
                    Background="{StaticResource Brush.Chrome}"
                    BorderBrush="{StaticResource Brush.BorderSubtle}" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock x:Name="LblCopied" Text="Results copied"
                               VerticalAlignment="Center" Margin="0,0,12,0"
                               FontSize="{StaticResource FontSize.Xs}"
                               Foreground="{StaticResource Brush.TextMuted}"
                               Visibility="Collapsed"/>
                    <Button Style="{StaticResource Style.Button.Dialog.Secondary}"
                            Content="Copy results" Click="BtnCopy_Click" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource Style.Button.Dialog.Primary}"
                            Content="Close" Click="BtnClose_Click" IsDefault="True"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
