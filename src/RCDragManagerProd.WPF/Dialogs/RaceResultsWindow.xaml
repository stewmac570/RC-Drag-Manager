<Window x:Class="RCDragManagerProd.WPF.Dialogs.RaceResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Class results"
        Width="1040" Height="660" MinWidth="760" MinHeight="520"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        KeyDown="Window_KeyDown"
        Background="{StaticResource Brush.Background}"
        FontFamily="Segoe UI"
        ShowInTaskbar="False">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="38" GlassFrameThickness="0"
                      ResizeBorderThickness="5" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <DataTemplate x:Key="MatchCardTemplate">
            <Border Width="210" MinHeight="94" Margin="0,0,0,10" Padding="12,10"
                    Background="{StaticResource Brush.Surface}"
                    BorderBrush="{StaticResource Brush.Border}" BorderThickness="1"
                    CornerRadius="{StaticResource Radius.Panel}">
                <StackPanel>
                    <TextBlock Text="{Binding MatchLabel}"
                               Foreground="{StaticResource Brush.TextHint}"
                               FontSize="{StaticResource FontSize.Xs}"/>
                    <TextBlock Text="{Binding Driver1}" Margin="0,6,0,2"
                               Foreground="{StaticResource Brush.TextPrimary}"
                               FontSize="{StaticResource FontSize.Md}" FontWeight="Medium"/>
                    <TextBlock Text="{Binding Driver2}"
                               Foreground="{StaticResource Brush.TextSecondary}"
                               FontSize="{StaticResource FontSize.Md}"/>
                    <TextBlock Text="{Binding Winner}" Margin="0,8,0,0"
                               Foreground="{StaticResource Brush.Success}"
                               FontSize="{StaticResource FontSize.Sm}"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="RoundColumnTemplate">
            <StackPanel Width="230" Margin="0,0,22,0">
                <TextBlock Text="{Binding RoundLabel}" Margin="0,0,0,10"
                           Foreground="{StaticResource Brush.Primary}"
                           FontSize="{StaticResource FontSize.Lg}" FontWeight="SemiBold"/>
                <ItemsControl ItemsSource="{Binding Matches}"
                              ItemTemplate="{StaticResource MatchCardTemplate}"/>
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="PhaseLadderTemplate">
            <StackPanel Margin="0,0,0,24">
                <TextBlock Text="{Binding Phase}" Margin="0,0,0,14"
                           Foreground="{StaticResource Brush.TextPrimary}"
                           FontSize="{StaticResource FontSize.Xl}" FontWeight="SemiBold"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Rounds}"
                                  ItemTemplate="{StaticResource RoundColumnTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Border Background="{StaticResource Brush.Background}"
            BorderBrush="{StaticResource Brush.WindowEdge}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="38"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{StaticResource Brush.Chrome}">
                <TextBlock Text="Class results"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="12" Foreground="{StaticResource Brush.TextHint}" FontWeight="Medium"/>
                <Button Style="{StaticResource Style.WinCtrl.Close}"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Margin="0,0,14,0" Click="BtnClose_Click"/>
            </Grid>

            <Border Grid.Row="1" Padding="24,16"
                    BorderBrush="{StaticResource Brush.BorderSubtle}" BorderThickness="0,0,0,1">
                <StackPanel>
                    <TextBlock Text="{Binding EventName}"
                               Foreground="{StaticResource Brush.TextPrimary}"
                               FontSize="{StaticResource FontSize.Xxl}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Summary}" Margin="0,5,0,0"
                               Foreground="{StaticResource Brush.TextSecondary}"
                               FontSize="{StaticResource FontSize.Md}"/>
                </StackPanel>
            </Border>

            <TabControl x:Name="ResultsTabs" Grid.Row="2" Margin="18,12"
                        Background="{StaticResource Brush.Background}"
                        Foreground="{StaticResource Brush.TextPrimary}">
                <TabItem Header="Ladder">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12,18">
                        <ItemsControl ItemsSource="{Binding Phases}"
                                      ItemTemplate="{StaticResource PhaseLadderTemplate}"/>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="Results list">
                    <DataGrid ItemsSource="{Binding ResultRows}" AutoGenerateColumns="False"
                              IsReadOnly="True" CanUserAddRows="False" Margin="8,14"
                              Background="{StaticResource Brush.Background}"
                              Foreground="{StaticResource Brush.TextPrimary}"
                              RowBackground="{StaticResource Brush.Background}"
                              AlternatingRowBackground="{StaticResource Brush.Surface}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Phase" Binding="{Binding Phase}" Width="150"/>
                            <DataGridTextColumn Header="Round" Binding="{Binding Round}" Width="100"/>
                            <DataGridTextColumn Header="Race" Binding="{Binding Match}" Width="65"/>
                            <DataGridTextColumn Header="Pairing" Binding="{Binding Pairing}" Width="*"/>
                            <DataGridTextColumn Header="Winner" Binding="{Binding Winner}" Width="160"/>
                            <DataGridTextColumn Header="Loser" Binding="{Binding Loser}" Width="160"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <TabItem Header="Round Robin standings"
                         IsEnabled="{Binding HasRoundRobinStandings}">
                    <Grid Margin="8,14">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Without this the table reads as arbitrary: a driver on one
                             win and one loss sits on 7 points only because a bye is
                             worth 2. -->
                        <Border Grid.Row="0" Padding="14,11" Margin="0,0,0,12"
                                Background="{StaticResource Brush.Surface}"
                                BorderBrush="{StaticResource Brush.Border}" BorderThickness="1"
                                CornerRadius="{StaticResource Radius.Panel}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets"
                                           FontSize="14" VerticalAlignment="Top" Margin="0,1,10,0"
                                           Foreground="{StaticResource Brush.TextHint}"/>
                                <TextBlock Text="{Binding ScoringNote}" TextWrapping="Wrap"
                                           MaxWidth="880"
                                           FontSize="{StaticResource FontSize.Sm}"
                                           Foreground="{StaticResource Brush.TextSecondary}"/>
                            </StackPanel>
                        </Border>

                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding Standings}" AutoGenerateColumns="False"
                                  IsReadOnly="True" CanUserAddRows="False"
                                  Background="{StaticResource Brush.Background}"
                                  Foreground="{StaticResource Brush.TextPrimary}"
                                  RowBackground="{StaticResource Brush.Background}"
                                  AlternatingRowBackground="{StaticResource Brush.Surface}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Rank" Binding="{Binding Rank}" Width="60"/>
                                <DataGridTextColumn Header="Driver" Binding="{Binding Driver}" Width="*"/>
                                <DataGridTextColumn Header="W" Binding="{Binding Wins}" Width="50"/>
                                <DataGridTextColumn Header="L" Binding="{Binding Losses}" Width="50"/>
                                <DataGridTextColumn Header="Byes" Binding="{Binding Byes}" Width="60"/>
                                <DataGridTextColumn Header="Points" Binding="{Binding Points}" Width="80"/>
                                <!-- The sum written out, so the Points column can be
                                     checked rather than taken on trust. Replaces the
                                     "Opponent strength" column, which was a bare number
                                     that meant nothing on its own. -->
                                <DataGridTextColumn Header="How that adds up"
                                                    Binding="{Binding PointsWorking}" Width="260"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Only appears when drivers actually finished level. The
                             tiebreak rules stay out of sight until one decides a place. -->
                        <Border Grid.Row="2" Padding="14,11" Margin="0,12,0,0"
                                Background="{StaticResource Brush.Surface}"
                                BorderBrush="{StaticResource Brush.Border}" BorderThickness="1"
                                CornerRadius="{StaticResource Radius.Panel}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasTieNotes}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock Text="Drivers who finished level"
                                           FontSize="{StaticResource FontSize.Xs}" FontWeight="Medium"
                                           Foreground="{StaticResource Brush.TextHint}" Margin="0,0,0,6"/>
                                <ItemsControl ItemsSource="{Binding TieNotes}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,3"
                                                       FontSize="{StaticResource FontSize.Sm}"
                                                       Foreground="{StaticResource Brush.TextSecondary}"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </Grid>
                </TabItem>
                <!-- Winner: the completion board, reachable for the life of the class
                     and not only in the moment the last race is called. Its DataContext
                     is set in code-behind from ClassCompletionPresentationBuilder —
                     that builder reads this one, so it cannot be nested here. -->
                <TabItem x:Name="WinnerTab" Header="Winner"
                         IsEnabled="{Binding HasWinner}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="14,16">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.4*"/>
                                    <ColumnDefinition Width="14"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Padding="26,24"
                                        Background="{StaticResource Brush.Primary}"
                                        CornerRadius="{StaticResource Radius.Panel}">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="★" FontSize="34" Foreground="White"
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Text="CLASS CHAMPION" Margin="0,8,0,7"
                                                   FontSize="{StaticResource FontSize.Sm}" FontWeight="SemiBold"
                                                   Foreground="#CCFFFFFF" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding ChampionName}" TextWrapping="Wrap"
                                                   FontSize="30" FontWeight="Bold" Foreground="White"
                                                   HorizontalAlignment="Center" TextAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Grid Grid.Column="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="10"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Padding="18,15"
                                            Background="{StaticResource Brush.Surface}"
                                            BorderBrush="{StaticResource Brush.Border}" BorderThickness="1"
                                            CornerRadius="{StaticResource Radius.Panel}">
                                        <StackPanel>
                                            <TextBlock Text="2ND PLACE"
                                                       FontSize="{StaticResource FontSize.Xs}" FontWeight="SemiBold"
                                                       Foreground="{StaticResource Brush.TextHint}"/>
                                            <TextBlock Text="{Binding RunnerUpName}" Margin="0,6,0,0"
                                                       FontSize="{StaticResource FontSize.Xl}" FontWeight="SemiBold"
                                                       Foreground="{StaticResource Brush.TextPrimary}" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Grid.Row="2" Padding="18,15"
                                            Background="{StaticResource Brush.Surface}"
                                            BorderBrush="{StaticResource Brush.Border}" BorderThickness="1"
                                            CornerRadius="{StaticResource Radius.Panel}">
                                        <StackPanel>
                                            <TextBlock Text="{Binding ThirdLabel}"
                                                       FontSize="{StaticResource FontSize.Xs}" FontWeight="SemiBold"
                                                       Foreground="{StaticResource Brush.TextHint}"/>
                                            <TextBlock Text="{Binding ThirdName}" Margin="0,6,0,0"
                                                       FontSize="{StaticResource FontSize.Lg}" FontWeight="SemiBold"
                                                       Foreground="{StaticResource Brush.TextSecondary}" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Grid>

                            <TextBlock Text="Finals results" Margin="2,20,0,6"
                                       FontSize="{StaticResource FontSize.Xs}" FontWeight="Medium"
                                       Foreground="{StaticResource Brush.TextHint}"/>
                            <DataGrid ItemsSource="{Binding FinalsResults}" AutoGenerateColumns="False"
                                      IsReadOnly="True" CanUserAddRows="False"
                                      HeadersVisibility="Column" MaxHeight="200"
                                      Background="{StaticResource Brush.Background}"
                                      Foreground="{StaticResource Brush.TextPrimary}"
                                      RowBackground="{StaticResource Brush.Background}"
                                      AlternatingRowBackground="{StaticResource Brush.Surface}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Round" Binding="{Binding Round}" Width="130"/>
                                    <DataGridTextColumn Header="Pairing" Binding="{Binding Pairing}" Width="*"/>
                                    <DataGridTextColumn Header="Winner" Binding="{Binding Winner}" Width="170"/>
                                    <DataGridTextColumn Header="Loser" Binding="{Binding Loser}" Width="170"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <TextBlock Text="Other finishers" Margin="2,18,0,6"
                                       FontSize="{StaticResource FontSize.Xs}" FontWeight="Medium"
                                       Foreground="{StaticResource Brush.TextHint}"/>
                            <DataGrid ItemsSource="{Binding OtherFinishers}" AutoGenerateColumns="False"
                                      IsReadOnly="True" CanUserAddRows="False"
                                      HeadersVisibility="Column" MaxHeight="200"
                                      Background="{StaticResource Brush.Background}"
                                      Foreground="{StaticResource Brush.TextPrimary}"
                                      RowBackground="{StaticResource Brush.Background}"
                                      AlternatingRowBackground="{StaticResource Brush.Surface}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Rank" Binding="{Binding Rank}" Width="70"/>
                                    <DataGridTextColumn Header="Driver" Binding="{Binding Driver}" Width="*"/>
                                    <DataGridTextColumn Header="W" Binding="{Binding Wins}" Width="65"/>
                                    <DataGridTextColumn Header="L" Binding="{Binding Losses}" Width="65"/>
                                    <DataGridTextColumn Header="Points" Binding="{Binding Points}" Width="90"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

            </TabControl>

            <Border Grid.Row="3" Padding="24,12"
                    BorderBrush="{StaticResource Brush.BorderSubtle}" BorderThickness="0,1,0,0">
                <Button Style="{StaticResource Style.Button.Dialog.Primary}"
                        Content="Close" HorizontalAlignment="Right" Click="BtnClose_Click"/>
            </Border>
        </Grid>
    </Border>
</Window>
